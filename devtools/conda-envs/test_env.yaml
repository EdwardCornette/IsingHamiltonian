name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  

  # Package dependencies
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - networkx
  - matplotlib.pyplot

    # Pip-only installs
  #- pip:
  #  - codecov

